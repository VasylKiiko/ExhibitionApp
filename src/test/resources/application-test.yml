management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: never

# INFO ENDPOINT CONFIGURATION
info:
  app:
    name: test
    version: test
    encoding: UTF-8
    java:
      version: 1.8

logging:
  level:
    com.Kiiko.ExhibitionsApp.controller: debug

server:
  error:
    include-message: always

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Exhibition_db_test
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

app:
  user:
    email: vasylkiiko@gmail.com
    password: Qwerty123!
  exhibition:
    id: 1
  ticket:
    id: 1